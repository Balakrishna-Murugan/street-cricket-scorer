// Temporary script removed to avoid requiring axios in server build.
// If you need to call the API from a script, either install axios or use this simple node https example.

console.log('callApi.ts is intentionally left as a no-op to avoid adding axios as a dependency in server build.');

process.exit(0);
